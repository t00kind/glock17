# Secrets — NEVER commit
.env

# Runtime state (per-deployment, would cause dedup issues / leaks)
posted_ids.json
posted_ids_vk.json
marker.txt
marker_vk.txt
bridge_state.json
crosspost.log
*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Editor / junk
Untitled
py

# Local Claude Code settings
.claude/settings.local.json
